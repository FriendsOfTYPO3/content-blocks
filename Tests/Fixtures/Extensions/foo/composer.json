{
	"name": "typo3tests/foo",
	"type": "typo3-cms-extension",
	"description": "This is a dummy extension to host content blocks.",
	"license": "GPL-2.0-or-later",
	"extra": {
		"typo3/cms": {
			"extension-key": "foo"
		}
	}
}
